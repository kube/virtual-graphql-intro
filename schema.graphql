type Restaurant {
  id: ID
  name: String
  customers: [Customer]
}

type Customer {
  id: ID
  name: String
  restaurant: Restaurant
}

type Query {
  restaurant(id: ID): Restaurant
  customer(id: ID): Customer
}
